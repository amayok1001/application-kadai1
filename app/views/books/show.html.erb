<div class="flash">
<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
</div>

<p class="show-text"> <strong> Title:</strong> <%= @book.title %></p>
<p class="show-text"> <strong> Body:</strong> <%= @book.body %></p>
<br>

<div class="link-box">
<span class="show-link">
    <%= link_to 'Edit', edit_book_path(@book), class: "edit_#{@book.id}" %></span>
    |
<span class="show-link"><%= link_to 'Back', books_path, class: "back" %>
</span>